# 全栈升级候选（rebase 到上游 main + 我方 carry-forward 重放）。
#
# 这次升级的动机是解锁 H4（resident step-invariant args）：H4 实测收益大，但会撞
# 非确定性 `orch_error_code=8 TENSOR_WAIT_TIMEOUT`，根因是 simpler 上游 #1902
# （disjoint-view L3 依赖推导）。#1902 无法 backport（`git cherry-pick 9f1536df`
# 落到 `e2efebcb` 上 → `CONFLICT (modify/delete): src/common/task_interface/buffer.h
# deleted in HEAD`），所以整栈升级是唯一路径。
#
# 五仓都推到 fork 的 `stepfun/upgrade-20260822` 分支；`stepfun/develop` **未动**，
# 保留成回退点。镜像过门之后才移 `stepfun/develop`。
#
# 组件由来：
#   pypto     143ea205 = 上游 main merge d43c8622 + 两次 simpler gitlink bump
#                        + `fix(runtime)` IPC interior 指针 provenance（见下）
#   simpler   85a82c45 = 上游 93adc386 + 我方 import_ipc_all/memset_all 移植
#                        （opcode 18/19 → 25/26,因为上游把 18 占成了
#                        _CTRL_COMMITTED_DEVICE_MEMORY）+ 2 个 UT 对齐,UT 15/15
#                        + `imported_region_buffer()`（见下）
#   pypto-lib 26977738 = 7545c454（landed.md 里最后一个有完整门证据的 SRC）
#                        + 同一个上游 merge 634e77b + IPC map provenance 接线。
#                        models/step3p5 与已验收版逐字节相同
#                        （decode_fwd.py a17ae274…,0 文件差异）。
#                        **不是** 全量 merge 49816493 —— 那个多带 6 个无门证据的
#                        commit,保留在 stepfun/upgrade-20260822-fullmerge-unqualified。
#   pto-isa   cd4a3d3f = pypto/runtime/pto_isa.pin 声明值（不是 origin/main tip）
#   PTOAS-src 307d0484 = tag v0.57 的 commit,和二进制对齐（镜像里只做参考,不编译）
#   ptoas-bin v0.57     = pypto/toolchain/versions.env 里的 PTOAS_VERSION
#
# ⚠ 升级基座打破了零拷贝 IPC 的 dispatch —— 三仓各一个修复（不是绕路,是缺口）：
#   上游 pypto #2273（`8662deb9`）把 dispatch 改成 address-free wire ABI,descriptor
#   由 `arg.buffer.tensor()` 导出,于是 `DeviceTensor(peer_base + offset)` 这类池内
#   裸指针全被 provenance guard 拒绝,54 个 resident arg 一个都过不去。修复是给每个
#   切片铸独立 `VMM_WINDOW` Buffer（base = 切片 VA、不被 `worker.free` 回收、按
#   `(worker_id, ptr, nbytes)` memo 保证 identity 稳定）并按切片 base 登记 provenance
#   ⇒ 上游的 `_child_prov_check_dispatch` 一行都不用改。另修 `DeviceTensor.reshape`
#   丢 buffer（地址/字节数都不变,是纯缺陷）。详见 blockers.md UPGRADE-IPC-PROV。

#
# ⚠ 外部工具链两个 pin 都必须读上游声明,不能挑"最新"：
#   先前误取 ptoas v0.59 + pto-isa 3186c381（各自的最新）,结果 36 个 kernel 被
#   ptoas 拒绝：`'pto.tcvt' op requires explicit tmp ... when PlanMemory is skipped`。
#   `PTOMaterializeImplicitTmp.cpp` 在 tag v0.57 **不存在**,v0.59 有 12 处硬报错;
#   生产走 MemoryPlanner.PYPTO（ptoas PlanMemory 被跳过）正好命中那条分支,而 pypto
#   没有给这些 op 传 tmp 的通路。换回 v0.57 + cd4a3d3f 后 codegen 零拒绝。
#   详见 upgrade-20260821/PATCH-AUDIT.md Appendix C。
#
# 已过的门（都在 0162,便宜门方式：拿现网镜像挂新源码编,不先整镜像构建）：
#   构建门 cheap-gate-20260822-130617   rc=0,a2a3 产物齐,两个 onboard
#                                       libhost_runtime.so 的 DT_NEEDED 都含
#                                       libhcomm.so（上游已吸收 --no-as-needed）
#   编译门 compile-gate-20260822-232050 rc=0,canonical whole_decode_step3p5 在
#                                       生产 shape（num_blocks=512 → ctx 64K）
#                                       `[holder] compile OK`,零 ptoas 拒绝
#   移植 UT tests/ut/py/test_worker/{test_import_ipc,test_memset_all}.py 15/15
#   镜像门 verify-final.log             `IMAGE_VERIFY=PASS` —— 在镜像内(digest 定身)
#                                       跑 pypto-image-audit.sh + 补充断言：五仓 pin
#                                       clean、凭据 scrub、swimlane-reuse 能力
#                                       available+constructed、a2a3 产物是真 ELF 且
#                                       DT_NEEDED 含 libhcomm.so、无 a5/onboard、
#                                       src-pins mirror 无残留、无 path-local remote、
#                                       import 落在 baked tree、vLLM patch 在位
#
# 构建产物（**候选,未推 registry**）：
#   第一版 manifest sha256:1a564006654a9e2192950bd89c63d9904b8e1c2d8f85b1b0def2a61f8e9f978b
#   第二版 manifest sha256:43fafc02072aa9614411cec297c5a211ce7837b6f058c56c2b3ce01670d8b40f
#          （IMAGE_VERIFY=PASS + smoke PASS,但整网 liveness 撞 UPGRADE-IPC-PROV ⇒ 已被取代）
#   第三版 <本 spec,pins 已含三仓 IPC provenance 修复;digest 构建后回填>
#
# 整网 liveness 已在**源码 overlay**下过（开发证据,非发布证据）：
#   liveness-patched-20260823-010005  SINGLE_CHIP_HIDDEN_CI=PASS,rc=0,217.176s
#   8 步逐 token exact 6127→303→1207→19384→872→428→6127→4231→2636,稳态 ~0.045 s/step
#   五个补丁文件都是纯 Python,且镜像内 pre-patch 字节与源码逐字节一致 ⇒ 等价
#   "镜像 + 本补丁"。**发布口径仍需在镜像内无挂载重跑。**
#
# 还没跑（所以这**不是** release-qualified,只是候选）：
#   镜像内 liveness 复跑、精度（N=128 逐 token vs vanilla ≥95%,阻塞在没有 vanilla
#   oracle 在跑）、H4 复测、A/B/A、六 BS 64K matrix。全量回退基线仍是 Wave5。

#
# 两个构建期绕路（都在 Dockerfile 里,不是临时 hack）：
#   1. onboard a5 在钉死的 CANN 9.0.0-beta.1 上编不过（halGetDeviceInfo 未声明等)，
#      而 detect_buildable_platforms() 总是返回 a2a3+a5 且 CMake ALL target 没有
#      平台过滤 ⇒ 构建期 sed 掉 a5,之后 git checkout -- 还原。CANN 不能动
#      （Phase 16 三剑合璧),所以移除代价 = 换 CANN。详见 CASEBOOK C10。
#   2. 源码不再在构建期从 github clone,而是由 build.sh 把七个仓打成 bare mirror
#      （src-pins.tgz）进 build context。原因：唯一有足够核数编 simpler 的 0162
#      **没有公网出口**（直连 github TCP 超时 130s,代理主机名连 DNS 都解析不了),
#      本地 devbox 又只有 5 核 / 12 GB 余量。可复现性不降 —— git 对象名就是内容
#      哈希,Dockerfile 里五仓 rev-parse 断言照旧。详见 CASEBOOK B6。
#      构建时需要： SRC_PINS_DIR=<含五仓 checkout 的目录>
#                  SRC_PYPTO_LIB=<release 版 pypto-lib，即 7545c454 那份>
#
# 升级带来的一个 API 改名（不是回归,上游留了 deprecation alias）：
#   RunConfig.enable_l2_swimlane → enable_chip_swimlane（pypto #2400 `5a4d79b7`）。
#   旧名仍能传（runner.py 里 pop 成新名 + DeprecationWarning),pypto-lib 侧本来
#   就已经用新名。只有 pypto-image-audit.sh 用 `fields(RunConfig)` 查名字,所以
#   它误报了一次 "required l2_swimlane_reuse_dep_gen is unavailable" —— 已改查新名。
#
# 构建（0162,192 核,buildkitd 已在跑,JOBS 压到 24 以免占满 CPU）：
#   CONTAINER_CLI="sudo -n /mnt/persist/k8s-install/containerd/bin/nerdctl" \
#   PTOAS_BIN_SRC=<v0.57 解包目录> SRC_PINS_DIR=<五仓目录> \
#   SRC_PYPTO_LIB=<release pypto-lib> BUILD_JOBS=24 \
#   bash build.sh builds/stepfun-upgrade-20260822.env
IMAGE_TAG=stepfun-upgrade-20260822
BASE=hub.i.basemind.com/stepcast/vllm-pypto@sha256:3d6392588fe9fb6ce4f5852100667d24f09d70f262dbd0ebe6c45b380f49573a
PYPTO_COMMIT=143ea2050f4303d053e4979eb3d4c58552214b19
PYPTO_LIB_COMMIT=26977738707f32c8a039d54ffbb074dd67fd4284
PTO_ISA_COMMIT=cd4a3d3f7a1a27fcfe536f617e9bca3008929664
PTOAS_COMMIT=307d0484a9e7d5e36f01b253d2bebe4d2f45fe81
SIMPLER_COMMIT=85a82c454074c069315ed6485033c3c2b136e562
PTOAS_BIN_VER=v0.57
# bin/ptoas 的内容 sha256（build.sh 从 ptoas-bin.tgz 里 `./bin/ptoas` 取,可复现）。
PTOAS_BIN_SHA256=2183e4cf00fd019403825290233c32b84c1b9904474ec4614ab976dac143aaae
# 故意不设 PTOAS_BIN_ARCHIVE_SHA256：那一项校验的是 build.sh 本地重打的
# ptoas-bin.tgz,而 `tar czf` 会把时间戳写进 gzip 头,不可复现。
# 上游 release asset（hw-native-sys/PTOAS v0.57 ptoas-bin-x86_64.tar.gz)的 sha256
# 记在这里作溯源：ea53c268c31ce3ad3aad45b25c35782562e19e570e156f5dd2b1bf2addb4fc79
ATTN_TASK_PROFILE=a2a3
REQUIRE_L2_SWIMLANE_REUSE_DEP_GEN=1
BUILD_JOBS=24
VLLM_PATCH_BRANCH=csy/pypto-tail-mtp-integration
VLLM_PATCH_COMMIT=1b3e538c35999e62b6d24e0651b3a85b7d16c826
